# 2026-05-12 工作记录

## 散点随地图缩放 - space.html
- 在 `updateScatterMap()` 的 `setOption` 之后，注册 `georoam` 事件监听
- 监听到缩放时读取 `geo[0].zoom`，按比例重新计算 `symbolSize`，范围限制 [4,28]
- `effectScatter` 的 symbolSize 同步调整，范围 [3,22]
- 基础缩放 `BASE_ZOOM=1.15`，基础点大小 `BASE_SIZE=10`

## 3D展示页建筑选择器优化 - 3d_architecture.html
- 为 `.building-selector-overlay` 添加悬浮层样式（绝对定位、固定宽度280px、右侧悬浮、毛玻璃背景）
- 建筑卡片图片高度从120px缩至80px，padding从15px缩至10px，减少视觉占用
- 添加 `selector-toggle-btn` 切换按钮，支持展开/收起建筑列表
- 面板展开时按钮紧贴面板左边缘（`right: 300px`），收起时按钮贴到最右边（`right: 0`，左圆角 `8px 0 0 8px`）
- 切换逻辑由 `toggleSelector()` JS函数控制，同步更新按钮图标（☰/✕）和样式
